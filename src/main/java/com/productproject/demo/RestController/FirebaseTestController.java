package com.productproject.demo.RestController;
/* 
package com.productproject.demo.Security;


import com.google.firebase.auth.FirebaseAuth;
import com.google.firebase.auth.FirebaseAuthException;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class FirebaseTestController {

    @GetMapping("/test")
    public String testFirebase() {
        try {
            // call to Firebase Authentication API
            String uid = FirebaseAuth.getInstance().createCustomToken("test-uid");
            return "Firebase is working! Custom Token: " + uid;
        } catch (FirebaseAuthException e) {
            return "Error: " + e.getMessage();
        }
    }
}
*/

